# Tools managed by mise. The host's own /home/utils Python/Node/UV are kept
# in PATH and used by default; mise fills the gaps (notably bun, missing on BAN).
# Run `mise install` after bootstrap to materialize these.

[tools]
bun = "latest"

[settings]
auto_install = true
quiet = true
